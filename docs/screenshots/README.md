# Screenshots

Generated by `make screenshots` (`node scripts/screenshots.mjs`, WP6) from
`omniwatch demo`, headless, at 1440×900 @2x, animations disabled. Naming:
`<name>-<theme>.png`, `theme` ∈ `dark`, `light`.

Names referenced from `README.md` and `docs/USER_GUIDE.md`: `split`, `list`,
`grid`, `zoom`, `usage`, `palette`, `quick-reply`, `onboarding`, `projects`,
`empty-not-running`, `compact`, `stats`, `timeline`, `settings`.

This file is a placeholder so the directory exists and the docs' relative
links resolve before the screenshots themselves are generated.
